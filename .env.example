# ─────────────────────────────────────────────────────────────────────────────
# Metagame 2026 — environment variables
#
# Copy this file to `.env.local` and fill in the values:
#     cp .env.example .env.local
#
# `.env.local` is gitignored — never commit real secrets. Missing *required*
# vars print a warning on `next dev` / `next build` (see src/env.ts). The site
# still runs without them; email signups just won't be stored.
# ─────────────────────────────────────────────────────────────────────────────

# --- Airtable (email signups + ticket purchases) ------------------------------ [required]
# Only the access token is secret and belongs here. The base/table ids and field names
# aren't secret (useless without the token), so they live in src/lib/airtable-config.ts.
# Token: https://airtable.com/create/tokens — scope `data.records:write`, granted access
# to the Metagame 2026 base only. Paste it into .env.local (NOT here).
AIRTABLE_API_KEY=

# --- Stripe (ticket webhook) -------------------------------------------------- [optional]
# Only the early-bird ticket *webhook* (recording purchases to Airtable) needs these.
# The Payment Link checkout itself is fully hosted by Stripe and needs none of it.
# Set the *test* values locally — the Stripe CLI prints the whsec_:
#   stripe listen --forward-to localhost:3000/api/stripe-webhook
# Use the *live* values in production. Without these the webhook gracefully no-ops.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- Supabase (not used yet) -------------------------------------------------- [optional]
# Placeholders only — nothing reads these today. Uncomment + fill if/when we
# add Supabase, and register them in src/env.ts so the check covers them.
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
