Parked, not deleted. The leading underscore makes this a Next.js private folder,
so `/night-market` no longer routes. To restore: `git mv` this back to
`src/app/night-market`, re-add the homepage teaser and the `night-market`
entry in `src/components/site/sections.ts`.
