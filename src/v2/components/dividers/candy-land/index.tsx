import IconDivider from "../IconDivider";
import { ICONS } from "./icons";

// lollipop · candy cane · crown · licorice — Candy Land (Noun Project, CC BY
// 3.0). Baked-in attribution stripped; credit lives on /credits.
export default function CandyLandDivider() {
  return <IconDivider icons={ICONS} />;
}
