#!/bin/sh
# Fresh worktrees often have no node_modules; CI's format:check is the backstop.
if [ ! -x node_modules/.bin/lint-staged ]; then
  echo "pre-commit: lint-staged not installed, skipping format (run pnpm install)" >&2
  exit 0
fi
exec node_modules/.bin/lint-staged
